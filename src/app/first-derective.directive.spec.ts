import { FirstDerectiveDirective } from './first-derective.directive';

describe('FirstDerectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new FirstDerectiveDirective();
    expect(directive).toBeTruthy();
  });
});
